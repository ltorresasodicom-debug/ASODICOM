[project]
name = "sigel-scraper"
version = "1.0.0"
description = "SIGEL — Scrapy spiders para portales LOTAIP, SERCOP, Finanzas e INEC"
license = { text = "AGPL-3.0-or-later" }
requires-python = ">=3.11"
dependencies = [
    "scrapy>=2.11.0",
    "scrapy-playwright>=0.0.34",
    "psycopg[binary]>=3.1.0",
    "structlog>=24.1.0",
    "python-dateutil>=2.9.0",
    "pdfplumber>=0.10.0",
    "httpx>=0.27.0",
    "tenacity>=8.2.0",
]
